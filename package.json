{
  "name": "list-of-instruments-and-effects-icon",
  "private": true,
  "type": "module",
  "description": "Icon rendering only -- the script itself needs no build step.",
  "scripts": {
    "icon": "node scripts/icon.mjs"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  }
}
